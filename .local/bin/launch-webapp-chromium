#!/bin/bash

# Launch the passed in URL as a web app in the chromium browser
exec setsid uwsm-app -- $(sed -n 's/^Exec=\([^ ]*\).*/\1/p' {~/.local,~/.nix-profile,/usr}/share/applications/chromium.desktop 2>/dev/null | head -1) --app="$1" "${@:2}"
